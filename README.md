# Template `raylib` Project

## Build Using CMake
1. Move to build directory `cd build/`
2. Call CMake on the CMakeLists.txt at root `cmake ../`
3. Build in the current directory according to the policy `cmake --build .`
4. Launch the executable `./game`
